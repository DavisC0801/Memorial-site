#!/bin/bash
####################################################
# Run Rake Command in Local Docker Environment
#
# Usage:
# bin/docker/rspec [OPTIONS]
# Options
# Any option supported by rspec, is passed through

set -e

docker-compose run rails bundle exec rake $@